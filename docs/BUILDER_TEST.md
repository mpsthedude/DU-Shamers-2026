# Live builder test

The service-only builder_test_accounts table routes enrolled identities to DU Shamers Test through actor_league_name. The ordinary member, props, and analysis endpoints use this route; clients cannot choose a league. Public dashboards, ESPN sync, and tracker workers remain scoped to DU Shamers. The commissioner API accepts scope=test only after real-league commissioner authorization; test writes allow only placement, settlement, and rejection against the fixed test season.

The Yahoo test owner receives simulated awards in its own 2026 season. Slot 1 tested the champion full-wager rule; slot 2 allows either $50/$50 or $100. Its window uses the normal Tuesday-to-Sunday cutoff (Sunday 11 AM Eastern). Fresh analysis runs retain the global five-per-award limit, and all provider budget gates still apply. Submissions use the real validation and persistence workflow in the test season. The Gmail commissioner has a test-league commissioner membership, while Yahoo stays an owner. Test submissions notify the commissioner's existing real-league devices with a TEST ONLY message. No wagers are placed automatically.

In the Gmail commissioner console, Test Tickets lists pending submissions and simulated bets. Enter simulated combined odds and select Simulate placement, then Simulate win or Simulate loss. These use the normal accounting RPCs with the isolated season. Refresh ticket queues reloads both lists. Real queue counts and public balances exclude test tickets. Database rollback validation covered placement and loss settlement and checked that production ledger rows remained identical.

The yellow TEST ACCOUNT banner identifies the session; the clubhouse still shows production league results. Sign out of the commissioner account and sign in through the Yahoo invitation to test. After submission, contact the commissioner/developer to arrange another isolated test award; there is no automatic reset. Remove the test enrollment and deactivate the private owner-directory row to retire access.

Validation performed: full Node suite; rollback-only analysis reservation; rollback-only ticket submission into the isolated season and rejection when targeting the production season. Rollbacks preserve all five real test analysis runs. Actual email confirmation and interactive end-to-end testing must be completed by the owner.
